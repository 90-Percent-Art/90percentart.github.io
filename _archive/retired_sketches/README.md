# Retired sketches

Sketches in this folder are preserved for reference but are not loaded by `make.html`.

- `noodles.js` was retired after the useful noodle/path texture ideas were folded into `whirls.js`.
- `blendTest.js` was a browser blend-mode diagnostic, not a plotter drawing sketch.
